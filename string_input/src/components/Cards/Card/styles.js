import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  root: {
    minWidth: 45,
    minHeight: 32,
  },

  root1: {
    minWidth: 45,
    minHeight: 32,
    border: "2px solid blue",
  },
  
});
